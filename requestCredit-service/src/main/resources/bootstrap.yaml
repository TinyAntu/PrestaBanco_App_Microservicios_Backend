spring:
  application:
    name: requestCredit-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8093
  datasource:
    url: jdbc:postgresql://localhost:5432/PrestaBanco
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update