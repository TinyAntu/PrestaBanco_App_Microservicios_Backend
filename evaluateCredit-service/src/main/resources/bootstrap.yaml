spring:
  application:
    name: evaluateCredit-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/Evaluate
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true